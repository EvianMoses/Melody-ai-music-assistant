fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1  # also used by scripts/test_generation.py to call the local Ollama REST API (§3.7)
python-multipart==0.0.20
# Shared Melody utilities (copied to /app/shared_lib in the Dockerfile).
melody-shared-lib @ file:///app/shared_lib

# Database access for ingestion + embedding scripts (contracts.db_models).
SQLAlchemy==2.0.51
psycopg[binary]==3.2.13
pgvector==0.3.6
python-dotenv==1.2.1

# Local embedding generation (scripts/generate_embeddings.py, ADR-003).
# torch has no CPU-only marker here; in Docker prefer the CPU wheel index:
#   pip install torch==2.5.1 --extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.3.1
torch==2.5.1
