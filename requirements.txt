Flask==3.1.3
boto3==1.43.22
botocore==1.43.22
spotipy==2.26.0
python-dotenv==1.2.1
requests==2.32.5
alembic==1.18.5
SQLAlchemy==2.0.51
psycopg[binary]==3.2.13
pgvector==0.3.6
# Google OpenID Connect sign-in (Phase 5 §5.2). Authlib handles the discovery
# document, JWKS rotation, and state/nonce correlation -- deliberately not
# hand-rolled, since ID-token validation is a classic source of auth bugs.
Authlib==1.7.2
# Fernet encryption for OAuth tokens at rest (AUTH-006, ADR-007).
cryptography==49.0.0